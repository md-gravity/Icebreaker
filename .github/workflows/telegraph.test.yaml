name: Running @applications/telegraph tests

env:
  JWT_SECRET_KEY: secret-key

on:
  pull_request:
    paths:
      - 'applications/telegraph/**'
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 19
      - run: npm ci --workspace=@applications/telegraph --include-workspace-root
      - run: npm run prisma:generate --workspace=@applications/telegraph
      - run: npm run typecheck --workspace=@applications/telegraph
      - run: npm run lint --workspace=@applications/telegraph
      - run: npm run build --workspace=@applications/telegraph