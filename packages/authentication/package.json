{
  "name": "@packages/authentication",
  "version": "1.0.0",
  "main": "./build/index.js",
  "scripts": {
    "setup": "npm install --verbose && npm run build",
    "dev": "tsx watch src",
    "watch": "tsc --watch",
    "build": "tsc --build --clean && tsc",
    "===utils===": "===utils===",
    "typecheck": "tsc --noEmit true",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix"
  },
  "dependencies": {
    "@trpc/server": "^10.20.0",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "@packages/eslint-config-hub": "1.0.0",
    "@packages/ts-configs": "1.0.0",
    "@types/node": "^18.11.3"
  },
  "eslintConfig": {
    "extends": [
      "@packages/eslint-config-hub/.base.js"
    ]
  },
  "prettier": "@packages/eslint-config-hub/.prettierrc.js"
}
